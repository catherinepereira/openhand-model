# Python
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Datasets: too large for git; downloaded via scripts (Kaggle / direct URL)
data/raw/
data/asl-alphabet/
data/processed/
data/processed_alphabet/
data/processed_test/
data/processed_fingerspelling/
data/hand_landmarker.task

# Training outputs: regenerated by train.py / export_onnx.py
exports/best.pt
exports/checkpoints/
exports/training_curves.png
# CTC training outputs: too large for git (best.pt is ~25 MB, plus backups
# and intermediate experiments). Keep the exported ONNX checked in elsewhere
# (in openhand backend/models/artifacts/) so the backend has a working model.
exports/ctc/
# Per-run backup snapshots scattered anywhere under exports/
exports/**/*.bak
exports/**/*.pt.bak
# Keep exports/asl_classifier.onnx + exports/model_meta.json checked in so
# the backend has a working model without retraining.

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
